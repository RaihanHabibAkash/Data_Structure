#include <bits/stdc++.h>
using namespace std;

int main() {
    // 1. Constructor vector with size 0. O(1)
    // vector <int> v; 

    // 2. Constructor vector with (n) elements. O(n)
    // vector <int> v(10);

    // 3. Constructor vector<>(n,x) with (n) elements and all value with (x). O(n)
    // vector <int> v(10, 5);

    // 4. Constructor vector by coping another one v2(v1). O(n)
    // vector <int> v(10, 5);
    // vector <int> v2(v);

    // 5. Constructor vector by coping all elements of an array. O(n)
    // int arr[5] = { 1, 2, 4, 8, 9 };
    // vector <int> v(arr, arr + 5);
    

    // 6. Create a vector like an array
    vector <int> v = {1, 3, 5, 7, 9};

    return 0;
}
