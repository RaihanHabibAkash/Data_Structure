#include <bits/stdc++.h>
using namespace std;

int main() {
    vector <int> v;

    // Return size of the vector.
    // v.size();

    // Return the max size vector can store.
    // v.max_size();

    // Returns the current capacity of vector. grows 2x.
    // v.capacity();

    // Clear the size of the vector. do not delete the memory.
    // v.clear();

    // Returns true/false if the vector is empty or not.
    // v.empty();

    // Resize the size of vector.
    // v.resize()

    return 0;
}


// #include <bits/stdc++.h>
// using namespace std;

// int main() {
//     vector <int> v;
//     v.push_back(10);
//     v.push_back(20);
//     v.push_back(30);
//     v.resize(5, 100);
//     for(size_t i = 0; i < v.size(); i++)
//         cout << v[i] << " ";
//     return 0;
// }